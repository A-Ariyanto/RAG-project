# Phase 0 + 1 + 2 dependencies. Later phases (FastAPI, SQLAlchemy) add their own
# deps as they land, per the roadmap.
asyncpg>=0.30
pydantic>=2.0
pydantic-settings>=2.0
pytest>=8.0

# Phase 1 — corpus scraper (ingestion/)
requests>=2.31

# Phase 2 — chunking + local embeddings (ingestion/). sentence-transformers
# pulls in a CPU torch; heavy but keeps ingestion API-key-free. numpy is used
# directly by ingestion/embed.py.
sentence-transformers>=3.0
numpy>=1.26
