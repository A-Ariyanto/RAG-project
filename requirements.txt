# Phase 0 + 1 dependencies. Later phases (FastAPI, SQLAlchemy, sentence-transformers)
# add their own deps as they land, per the roadmap.
asyncpg>=0.30
pydantic>=2.0
pydantic-settings>=2.0
pytest>=8.0

# Phase 1 — corpus scraper (ingestion/)
requests>=2.31
